# e_ai_concierge 评审
冒烟: PASS（`E smoke OK: 计划采纳/Check 写入/思考注册/高置信自动+撤销/低置信确认/落盘 全部通过`）
步骤数: 今日计划建议→采纳=2 次点击 · 目标+grill_me→运行→改后采纳=7 次输入（下拉×4+运行+改后采纳+编辑+确认，全用默认值可降至 3）· 丢文件→等 5s→撤销=2 动作+5s（watch_loop 固定 sleep 5s, app.py:275）
- 任务1 采纳写入了什么：逐行进今日计划（app.py:202-207），snackbar"已采纳 N 条"（app.py:186）；任务2 编辑路径顺畅：对话框预填全文（app.py:166-180）；任务3 实测（真实丢文件+临时 vault 全链路）：高置信落盘 1 条进度、撤销后归 0，含「[低置信]」文件走确认卡、落盘 0 条——三分类正确、service 层可逆。
基线对照: 8 条中符合 7 条；违背: #3 空态引导（时间流/空库无 hint()，app.py:118,393-397；ui_kit.py:68 的 hint 全文件未用）
摩擦点: P0 ×2 · P1 ×4
- P0-1 撤销只活在 6 秒瞬时 snackbar（ui_kit.py:63），信息卡文案却写"(可撤销)"且无按钮（app.py:298-301）——违背 DESIGN_CHECKLIST.md:24「通知卡必须带撤销」；错过 6 秒即永久不可逆，与"可放心托付"的定位相悖。
- P0-2 AI 调用同步阻塞且零进行中反馈：4 个入口同步 collect_text（app.py:200,218,238,261），watch_loop 也在协程内同步路由（app.py:278-279）；OpenAI 端超时 120s（runtime.py:90）→ 真实端点下点击后界面最长冻结 2 分钟、无 spinner；失败卡"(AI 无返回)"仍带采纳键，Check/周复盘采纳会把占位文本写盘（app.py:221,242）。fake 秒回掩盖全部问题。
- P1-1 采纳不消卡不禁用（app.py:140-153）：重复点「采纳」重复写计划项，无去重提示。
- P1-2 build_target_options 嵌套 break bug（app.py:70-74）：实测 2 项目+2 计划 → 「任务甲」在下拉重复 2 次、「任务乙」永不出现。
- P1-3 思考方式采纳只留首行进计划，其余分析内容无处保存（app.py:264-267），grill_me/六顶帽长输出等于丢弃；右栏标签"到点自动来的思路"（app.py:369）实际无任何定时器，阶段卡只能手动触发，文案不实。
- P1-4 时间流只增不减（app.py:153,317 只 insert，仅忽略移除 app.py:160），长会话控件无上限，滚动重渲染渐慢。
评分: 流畅 4 / 反馈 2 / 可发现 3 / 推荐 3
判词: 适合"AI 只产草稿、落盘必经人工确认"纪律性强、配快速本地端点做每日计划-复盘闭环的个人用户；接慢速真实端点、或指望高置信自动归档可以放心走开的场景，目前都不合格。
