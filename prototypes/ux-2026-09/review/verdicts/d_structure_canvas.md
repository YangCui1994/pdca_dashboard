# d_structure_canvas 评审
冒烟: PASS（`D smoke OK: 布局回环/网格补齐/坏文件回落/状态循环/停滞真实值/落盘 全部通过`）
步骤数: 拖拽存布局=1 次手势（松手即静默落盘 `ux-2026-09/canvas_layout.json`，重启后 `merge_layout` 恢复；手感参数 `drag_interval=10ms`，仅 `max(0,·)` 下界钳制、无吸附、无上界） · 开抽屉切状态=2 击（卡片→色块；snackbar+抽屉/卡片即时重渲染，app.py:217-227） · 发现停滞=画布 0 击见红框+停滞计数，1 击进抽屉见具体行动与真实天数（`today-last_meaningful_update_at`，app.py:203,206；阈值 15 天来自 models.py:52）
基线对照: 8 条中明确符合 2 条，N/A 3 条（无输入框/零 AI），违背 3 条——①统一反馈：拖拽落盘无提示（app.py:130-131）；②可撤销：undo_snackbar 导入未用（app.py:26），「适应窗口」一键清空全部手工布局且无确认（app.py:155-161,237）；③空态：0 项目时画布白板无 hint（app.py:93,148-153）。宁空勿假（app.py:79,102）、语义色板（app.py:198-199）符合。版本特有验收全过：布局重启保留/适应窗口回网格/停滞红框（截图证实）。
摩擦点: P0 ×2 · P1 ×4
- P0 画布固定高 560 且无滚动/缩放/minimap（app.py:93；3 列网格 dy=200）→ 约 ≥7 个项目第 3 行起被裁掉不可达，「空间总览替代列表导航」在真实规模下失效。
- P0 拖拽无上界钳制（app.py:125-126）→ 卡片可拖出可视区被裁剪丢失，唯一找回手段「适应窗口」又会清空全部布局——丢卡与破坏性恢复互锁。
- P1 「适应窗口(重置布局)」无确认无撤销（app.py:237），误触代价=重排全部卡片。
- P1 布局 JSON 合法但类型错（如数组）时 `stored.get` 崩溃（app.py:46-48 只兜 OSError/JSONDecodeError，app.py:66），坏类型回落不完整。
- P1 停滞红覆盖状态色（app.py:199）：停滞行动色块颜色不再反映真实状态（文字标签仍在）。
- P1 卡片只给停滞计数不给天数（app.py:105-106），判断严重程度必须逐卡进抽屉。
评分: 流畅 4 / 反馈 4 / 可发现 3 / 推荐 3
判词: 适合项目数 ≤6、靠空间记忆做周度总览与停滞巡检的个人用户；项目一多（无滚动）或误触「适应窗口」（无撤销）时，画布从资产变成负债。
