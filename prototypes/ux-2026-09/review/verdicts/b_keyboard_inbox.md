# b_keyboard_inbox 评审

冒烟: PASS（`B smoke OK: 排序/键位/转计划/重命名/直改状态/命令文档/落盘 全部通过`）

步骤数: 清空3条想法=3键（每条恰好1键,1/2/3处理后 selected 归零 app.py:107,无需j/k）· 不碰鼠标改行动状态=不可达（键盘0条路径;鼠标退路1击）· Ctrl+K找重命名键位=1步（空查询即全文档 app.py:183,读到 `e`——但按 `e` 无效）

基线对照: 8 条中符合 6 条；违背: #2 危险操作无撤销（undo_snackbar 仅 import app.py:24,归档键"2"误按即出队无后悔药 app.py:106）; #3 部分违背（行动列表无空态 app.py:288;队列/计划空态合格 app.py:238,254）

摩擦点: P0 ×3 · P1 ×3
- P0-1 `e` 键无处理器: README.md:7、命令文档 app.py:66、编辑钮 tooltip app.py:250 三处承诺"e 重命名",on_key(app.py:89-111)只处理 ctrl+k/c/j/k/1/2/3——重命名只能鼠标点图标,提示文案与实现不一致。
- P0-2 `s`/`d` 键无处理器: 同样只有行内 TextButton(app.py:282-283);栏目标题 app.py:341 与提示语 app.py:157("先用 s/d 或点击")都在暗示键盘路径存在。键盘流版本的核心任务2直接失败。
- P0-3 on_key 无输入焦点守卫(app.py:367): Flet 页面级键盘事件在 TextField 聚焦时仍触发,在捕获框/工作记录/重命名对话框/命令面板里打字含 c/j/k/1/2/3 会同时触发想法处理(如捕获含"2"的句子会把选中想法归档)。
- P1-1 命令面板无 Esc 关闭,按钮自述"Esc 不行就点这"(app.py:194)——键盘用户被迫摸鼠标。
- P1-2 行动"选中"仅字重变化(app.py:277),无边框/底色,扫视难确认选中了谁。
- P1-3 死代码 `if False` 残留(app.py:95);Ctrl+K 在对话框打开时无遮罩判断(归入P0-3根源)。

评分: 流畅 2 / 反馈 3 / 可发现 4 / 推荐 2
判词: 想法分拣的 touch-once 流(1键1条、卡片就地键位提示、面板即文档)是五版里最高效的收件箱;但在 s/d/e 三个被文档承诺的键位接上之前,它只是一个"想法分拣器",不能交给需要管理项目行动的键盘党。
